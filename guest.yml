---

- name: run the playbook tasks on the localhost
  hosts: localhost
  connection: local
  become: yes
#  become_user: jaels
  vars_files: [ 'roles/guest/vars/personal.yml' ]

  roles:
    - geerlingguy.ntp
    - andrewrothstein.kubernetes-helm
    - guest
    - geerlingguy.docker
    - andrewrothstein.docker-machine
    - gantsign.oh-my-zsh 
    - andrewrothstein.go
